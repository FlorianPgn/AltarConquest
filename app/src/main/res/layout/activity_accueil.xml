<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_accueil"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.florian.altarconquest.View.EcranAccueil"
    android:background="@drawable/fond" >

    <ImageButton
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:background="@drawable/bouton_regles"
        android:id="@+id/bouton_regles"
        android:layout_marginBottom="16dp"
        android:layout_above="@+id/bouton_credits"
        android:layout_alignStart="@+id/bouton_credits" />

    <ImageButton
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:background="@drawable/jouer"
        android:id="@+id/bouton_jouer"
        android:layout_above="@+id/bouton_regles"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="17dp" />

    <ImageButton
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:background="@drawable/bouton_credits"
        android:id="@+id/bouton_credits"
        android:layout_marginBottom="28dp"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/bouton_jouer" />

    <ImageView
        android:background="@drawable/altar_conquest"
        android:id="@+id/logo"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="23dp" />

</RelativeLayout>
